@using Microsoft.AspNetCore.Identity
@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager

	@if (SignInManager.IsSignedIn(User))
	{
		<a asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">
			<img alt="profile" class="header-icon hide-in-mobile" src="~/media/icons/user.png" />
		</a>
	}
	else
	{
			<a class="text-dark" asp-area="Identity" asp-page="/Account/Login">
			<img alt="log in" class="header-icon hide-in-mobile" src="~/media/icons/user.png" />
			</a>
	}